/**
 * code generated by JHelper
 * More info: https://github.com/AlexeyDmitriev/JHelper
 * @author jignesh1604
* Link :  https://codeforces.com/contest/959/problem/C
 */

#pragma comment(linker, "/stack:200000000")
#pragma GCC optimize("Ofast")
#pragma GCC target("sse,sse2,sse3,ssse3,sse4,popcnt,abm,mmx,avx,tune=native")

#include <bits/stdc++.h>

using namespace std;

class CMahmoudAndEhabAndTheWrongAlgorithm {
public:
	void solve(std::istream& in, std::ostream& out) {
	    long long int n , i;
	    in >> n;
	    if(n <= 5) {
	        out << -1 << "\n";
	        for(i = 1 ; i < n ; i++) {
	            out << i << " " << i+1 << "\n";
	        }
	    }
	    else {
	            for(i = 2 ; i <= n/2+1 ; i++) {
	                out << 1 << " " << i << "\n";
	            }
	            for(i = n/2 + 2 ; i <= n ; i++) {
	                out << 2 << " " << i << "\n";
	            }
	            for(i = 1 ; i < n ; i++) {
	                out << i << " " << i+1 << "\n";
	            }
	    }

	}
};


int main() {
	CMahmoudAndEhabAndTheWrongAlgorithm solver;
	std::ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	std::istream& in(std::cin);
	std::ostream& out(std::cout);
	solver.solve(in, out);
	return 0;
}
